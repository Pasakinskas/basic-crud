<!DOCTYPE html>

<html lang="en">
  <head>
    <% include ../partials/head %>
  </head>
  <body class="container">
    <header>
      <% include ../partials/header %>
    </header>
    
    <main class="page-body">
      <div class="text-center row">
        <h1>My to-do list:</h1>
        <div class="col-md-4"></div>
        <ul class="data-list col-md-4" id="quotesList">
        <% for(let i=0; i<quotes.length; i++) {%>
          <li class="row data-li">
            <div class="col-xs-3 todo-name"><span><%= quotes[i].name %></span></div>
            <div class="col-xs-6 todo-body"><span><%= quotes[i].quote %></span></div>
            <div class="col-xs-3"><button class="btn btn-delete" data-id="<%= quotes[i]._id %>">Solve!</button></div>
          </li>
        <% } %>
        </ul>
      </div>

      <div class="text-center block">
        <h2>Add a new To-do here:</h2>
        <form action="/quotes" method="POST" id="saveQuote">
          <input class="my-textbox" type="text" placeholder="name" name="name" required>
          <input class="my-textbox" type="text" placeholder="quote" name="quote" required>
          <button type="submit">Submit</button>
          <p class="on-demand" id="explain-form-error">Please only use lowercase letters and keep special symbols to ? and !</p>
        </form>
    </div>

      <div class="text-center block">
        <h2>Magical button of replacement</h2>
        <p>(Replaces cooking tasks, because they dont require willpower)</p>
        <button id="update">Activate!</button>
      </div>

      <script src="/static/main.js"></script>
  </main>

  <footer>
    <% include ../partials/footer %>
  </footer>
  </body>
</html>